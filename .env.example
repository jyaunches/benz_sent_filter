# Benz Sent Filter Configuration

# API Configuration
API_HOST=0.0.0.0
API_PORT=8002
LOG_LEVEL=INFO

# Uvicorn Worker Configuration
# Number of worker processes for handling concurrent requests (range: 1-16)
# - 1 worker: Single process (default, best for debugging with clear logs)
# - 4 workers: Local development with multiple concurrent requests
# - Production: Set via Dockerfile (2 workers per instance)
UVICORN_WORKERS=1

# Model Configuration
MODEL_NAME=typeform/distilbert-base-uncased-mnli
MODEL_CACHE_DIR=~/.cache/huggingface/transformers/

# Classification Thresholds
CLASSIFICATION_THRESHOLD=0.6
COMPANY_RELEVANCE_THRESHOLD=0.5
